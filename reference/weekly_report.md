# 周报 Prompt

先读 `report_principles.md`（三段式、结论>数量、覆盖工作内容/进度/风险）。周报要求
与日报相同，区别在于**跨天聚合**。

## 输入（两种来源，二选一）

**A. 会话采集结果**（默认）：整周（周一至周日）的 `days` 按日期分组的 session 桶。

**B. 用户提供的日报**（"发日报→生成周报"）：用户直接把本周的若干份日报（文本/粘贴/文件）发来，
要求据此生成周报。此时**以用户的日报为唯一事实来源**，不再去翻会话记录（除非用户要求补充）：
- **若是多人共享的团队日报，先做「人物提取」**（见 `report_principles.md`）：只保留本人（或用户指定者）
  名字开头的条目，丢掉其他同事的，别误抓相邻行。
- 解析每份日报里的条目（项目、产出/结论、下一步、风险）。
- 跨天合并同一项目/主题为一条进展弧线，抽象层级高于单日。
- 风险按各日报的风险汇总、去重，合进周报「风险同步」。
- 不要新增日报里没有的事实；日报缺失的结论在周报里仍标为缺口。

## 处理
1. **跨天合并同一线程**：同一个项目/文件/任务/主题在多天推进时，合并成**一条**，
   用进展动词描述整段弧线（"从…推进到…""逐步完成…"），不要按天拆成多条。
   匹配看：项目/计划的规范名、同一文件/模块、同一反复出现的主题——不只看 ID 全等。
2. 每条仍按三段式：做了什么（整周累计）→ 产出/结论 → 下一步。
3. **抽象层级高于单日**：周报描述比任何一天都更概括。
4. **保留多样性**：进展、决策、风险、协作/外部信息都要体现，不要全压成"进展"。
5. 按重要度排序（影响/决策性/风险优先），再按覆盖广度（涉及天数/会话数）。
6. 若整周只有一天有活动：当作"本周开局快照"，少而精、抽象写。

## 输出结构（Markdown，给人读）

措辞专业、保留英文术语、不出现代码字段、不用 emoji（见 `report_principles.md`）。
分**公司周报**和**个人周报**两份**独立**文档，**每份都含完整的 5 个板块**
（总结 / 本周完成 / 进行中 / 下周重点 / 风险同步全部公司归公司、个人归个人，不要全局共享）。
「本周完成」按主题分节、按重要性排序，每节密实、带指标与链接状态；有对比数据用小表格。
下面是**单份**的模板（公司、个人各套用一次）：

```markdown
# 周报 · {week_start} ~ {week_end}

## 总结
1–2 句主线：本周核心进展/结论。

## 本周完成
### \<主题/项目（保留英文术语）\>
做了什么 + 关键指标/结论（密实要点，可多条）。下一步若紧跟可一句带过。
相关：[!387 一句话说明](…) — 已合并 ✅；[Notion 文档](…)

### \<下一个主题\>
（有评测/对比时用表格）

| 模型 | 指标 |
|---|---|
| … | … |

## 进行中
- \<正在推进、尚未完成的事\>

## 下周重点
- \<下周关键事项，按优先级\>

## 风险同步
- **\<风险点\>**（进度/延期/依赖/待确认/质量/成本/安全）：什么有风险 + 影响 + 应对。
- …（无则"本期无重大风险"）
```

「本周完成」按重要性排序（主项在前，可在标题后标注（主线）/（调研）/（支线）作提示）。
同一项目跨多天合并成一条进展弧线（progression）。风险既在对应条目里点到，也在末尾「风险同步」汇总。

## 出完报告后：主动询问归类

报告给出后，**主动用 AskUserQuestion 问用户**确认公司/个人归属是否正确。可先按
`project_categories.md` 给预判。拿到回答后据需要重排，并把归属写回 `project_categories.md` 与记忆。

## 自检
- [ ] 总结 1 句、不复述下面各条？
- [ ] 同一线程跨天合并成一条，没有按天重复？
- [ ] 每条都有"产出/结论"而非数量堆叠？每条约 3 句？
- [ ] 公司/个人各一份独立报告，各自带全套板块（含进行中/下周重点/风险同步）？
- [ ] 按重要度排序、不杜撰、不暴露代码字段/session_id/路径？链接确属本条产物？
- [ ] 整周无活动则说明并返回空？

## 范例（公司周报 few-shot；通用脱敏样例，仅示范结构与文风）

```markdown
# 周报 · 2026-XX-XX ~ 2026-XX-XX

## 总结
本周以文件类内容缓存降本上线为主线，并行推进召回实验、模型蒸馏评测复核，及一项安全方案、一例线上 P1 与一次数据时区改造。

## 本周完成

### 1. 文件类内容缓存（L1+L2）降本——从方案到上线再到实测（主线）
背景：文件类内容（网盘/笔记/附件）记忆占明细成本较高。本周走完完整弧线：与同事对齐 L1（LLM node cache）+ L2（datasource cache → data_id → 向量库召回历史 memory）两级框架 → 完成三类文件部署上线，解决 review 多处 conflict（去硬编码、prompt override、处理逻辑版本进 key、data_id 优先），本地 59 单测全过、线上召回链路打通 → 接入监控实测。结论：真实命中率约 27%（峰值约 32%），仅 L2 一层在约 7.35 小时窗口节省约 4500 万 token，约 73% 请求仍重算；L2 仅对上线后新写记录生效，旧记录缺新增字段无法匹配。
相关：[MR#xxx](…)
下一步：用累计标量补全总节省 token 硬数字；评估旧记录回填以覆盖历史降本。

### 2. 多层披露召回实验（主线/调研）
跨天迭代：修复召回节点分类两处问题（行级→节点级，避免同次调用不同类型节点互相覆盖；修正字段拼写）→ 与同事对齐改进 prompt 总召回 63 vs 原 50（+26%）→ 在线复盘（149 条、46 次检索）整体召回分 +2.25pp、正向，但 weekly 层文本召回率下降、summary 更长 → 第一版优化，第一层自进化 6 天数据（偏稀疏）：

| 层级 | 检索提升 |
|---|---|
| daily | +106.7% |
| weekly | +71.1% |
| monthly | +0% |
| total | +69.4% |

下一步：引入 prefetch 并改进方案；重点提升 weekly/monthly 覆盖率，导出召回内容算语义相似度验证质量。

### 3. 模型蒸馏评测复核定档（调研/评测）
重跑蒸馏 judge 并修正图片 judge 评测 prompt（对可见文字/品牌误识别加重罚、补自然图片），产出 54 条新评测 + 逐条打分 + 极端 case 复核。结论（均分）：

| 类型 | 排序 |
|---|---|
| 文档类 | teacher 4.364 > SFT-A 3.809 > online 3.705 > SFT-B |
| 图片类 | teacher 3.759 > online 3.741 > SFT-B 3.481 > SFT-A 3.284 |

bad case 集中在小字误读、长文本重复失控。下一步：重点关注图片型 pdf 效果，审 badcase 避免影响最终 judge。

### 4. 安全方案 + 全链路改进调研（调研/设计）
厘清现有过滤仅做噪声过滤、非投毒防御后，产出一版可落地、高 ROI 的防御方案（provenance 信号 + 多源印证），覆盖协同投毒绕过等 case；同时逐节点通读 pipeline 形成完整认知，并调研最新论文梳理改进方向。判定 eval > 投毒封顶 > 调度。下一步：与负责相关模块的同事对齐封顶执行边界。

### 5. 数据正确性修复（支线）
排查送数仓供 BI 分析的字段，确认仅传字符数，明确空值/哨兵值保留原样的边界，已提 MR。
相关：[MR#xxx](…)

### 6. 线上 P1：大文件摘要超时（支线）
大文件摘要请求在 90s 节点超时处被取消（未达 client 180s 上限），代理层报 client_disconnect 触发 P1——非模型故障，但供应商已计费。已将超时 90→150s 让请求跑完用上结果，配置已发布。下一步：监控告警降幅确认修复生效。

### 7. 多时区拆分改造（支线）
排查数据例行任务失败根因：上游字段由 local 改为 regional 致依赖标记匹配失败。已修复标记，用户按注册时间拆两组时区，下游脚本逐用户传 tz_offset，例行任务拆为 8 个（日/周/月/删除 × 两时区）。下一步：观察日报产出完整性。

## 进行中
- 缓存降本量化（累计硬数字）与旧记录回填评估。
- 召回实验 prefetch 接入与第二版方案。
- 安全方案与相关模块团队对齐封顶执行边界。

## 下周重点
- 推动缓存降本硬指标产出，评估历史记录回填。
- 召回实验引入 prefetch、提升 weekly/monthly 覆盖率与召回质量验证。
- 蒸馏图片类 pdf 复核，推动最终 judge 定档。
- 安全方案落地 provenance 信号，跨团队对齐封顶边界。

## 风险同步
- **质量（降本）**：命中率约 27%、约 73% 请求仍重算；L2 仅对上线后新写生效，历史降本未覆盖，需评估回填。
- **质量（召回）**：monthly 层 +0%、weekly 层文本召回率下降，覆盖率未达预期；6 天数据偏稀疏，结论需继续积累。
- **待确认（线上）**：超时修复、多时区拆分均待观察（告警降幅 / 日报产出完整性）确认生效。
- **依赖**：BI 修复 MR 待评审合入。
- **跨团队待确认**：封顶执行落在相关模块侧，边界需对齐，存在方案返工可能。
```
